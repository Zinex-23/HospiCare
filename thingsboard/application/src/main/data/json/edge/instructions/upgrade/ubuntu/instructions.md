#### Upgrading to ${TB_EDGE_VERSION}EDGE

HospiCare Edge package download:
```bash
wget https://github.com/hospicare/hospicare-edge/releases/download/v${TB_EDGE_TAG}/tb-edge-${TB_EDGE_TAG}.deb
{:copy-code}
```
Install package:
```bash
sudo dpkg -i tb-edge-${TB_EDGE_TAG}.deb
{:copy-code}
```

${UPGRADE_DB}
