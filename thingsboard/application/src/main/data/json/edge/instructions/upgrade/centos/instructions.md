#### Upgrading to ${TB_EDGE_VERSION}EDGE

HospiCare Edge package download:
```bash
wget https://github.com/hospicare/hospicare-edge/releases/download/v${TB_EDGE_TAG}/tb-edge-${TB_EDGE_TAG}.rpm
{:copy-code}
```

Install package:
```bash
sudo rpm -Uvh tb-edge-${TB_EDGE_TAG}.rpm
{:copy-code}
```

${UPGRADE_DB}
